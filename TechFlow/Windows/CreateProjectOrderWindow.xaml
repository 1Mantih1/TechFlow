<Window x:Class="TechFlow.Windows.CreateProjectOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Создание заказа проекта" Height="750" Width="900"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#1b2339" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Стили из EditProfilePage -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="0,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="8" Padding="5">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" />
                                    <Border x:Name="UnderlineBorder" Background="#5fc4b8" Height="2" VerticalAlignment="Bottom" Margin="0,0,0,-2" Opacity="0" />
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCalendarStyle" TargetType="Calendar">
            <Setter Property="Background" Value="#252c43" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#5fc4b8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Calendar">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="8">
                            <CalendarItem x:Name="PART_CalendarItem" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="0,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="8" Padding="5">
                                <Grid>
                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                                   Foreground="{TemplateBinding Foreground}" 
                                                   FontSize="{TemplateBinding FontSize}" 
                                                   FontFamily="{TemplateBinding FontFamily}" 
                                                   VerticalContentAlignment="Center" 
                                                   HorizontalContentAlignment="Left" 
                                                   Padding="10,5" 
                                                   Background="Transparent" 
                                                   BorderThickness="0" 
                                                   Margin="0,0,40,0" 
                                                   IsReadOnly="True"/>
                                    <Border x:Name="UnderlineBorder" Background="#5fc4b8" Height="2" VerticalAlignment="Bottom" Margin="0,0,0,-2" Opacity="0" />
                                </Grid>
                            </Border>
                            <ToggleButton x:Name="PART_Button" 
                                       HorizontalAlignment="Right" 
                                       VerticalAlignment="Center" 
                                       Width="30" 
                                       Height="30" 
                                       Margin="0,0,5,0" 
                                       Background="Transparent" 
                                       BorderBrush="Transparent" 
                                       Focusable="False">
                                <ToggleButton.Content>
                                    <materialDesign:PackIcon Kind="Calendar" 
                                                         VerticalAlignment="Center" 
                                                         HorizontalAlignment="Center" 
                                                         Foreground="#5fc4b8" />
                                </ToggleButton.Content>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" 
                               Placement="Bottom" 
                               PlacementTarget="{Binding ElementName=PART_Button}" 
                               StaysOpen="False"
                               IsOpen="{Binding IsChecked, ElementName=PART_Button}">
                                <Border Background="#252c43" 
                                    BorderBrush="#5fc4b8" 
                                    BorderThickness="1" 
                                    CornerRadius="8" 
                                    Padding="5">
                                    <Calendar x:Name="PART_Calendar" 
                                          DisplayDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}}" 
                                          SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}}" 
                                          Style="{StaticResource ModernCalendarStyle}" />
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                        <Grid>
                            <Grid x:Name="WatermarkContent" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="0" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <Grid>
                                <Border x:Name="gradientBorder" 
                                        Background="#252c43" 
                                        CornerRadius="5">
                                </Border>
                                <ContentPresenter x:Name="contentPresenter" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="gradientBorder">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#7fd8cc" Offset="0" />
                                            <GradientStop Color="#5caa95" Offset="1" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Opacity" Value="0.9" TargetName="gradientBorder" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="gradientBorder">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#4fa89c" Offset="0" />
                                            <GradientStop Color="#3a7c6a" Offset="1" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Opacity" Value="0.8" TargetName="gradientBorder" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="#a0a8c0" />
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#5fc4b8" Offset="0" />
                        <GradientStop Color="#3d8f7a" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,20,0,10" />
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Margin" Value="0,10,0,5" />
        </Style>

        <Style x:Key="ModernCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e8ff"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="checkBoxBorder" 
                                    Width="20" Height="20" 
                                    Background="#252c43" 
                                    BorderBrush="#5fc4b8" 
                                    BorderThickness="1" 
                                    CornerRadius="4"
                                    Margin="0,0,10,0">
                                <Path x:Name="checkMark" 
                                      Data="M0,6 L3,9 L8,2" 
                                      Stroke="#5fc4b8" 
                                      StrokeThickness="2" 
                                      Stretch="Fill"
                                      Visibility="Collapsed"/>
                            </Border>

                            <ContentPresenter Grid.Column="1" 
                                          VerticalAlignment="Center"
                                          Margin="5,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="#7fd8cc"/>
                                <Setter TargetName="checkMark" Property="Stroke" Value="#7fd8cc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="8" Padding="5">
                                <Grid>
                                    <ToggleButton x:Name="ToggleButton" 
              Grid.Column="2" 
              Focusable="false"
              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
              ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="20"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.ColumnSpan="2" Background="Transparent"/>
                                                    <Border Grid.Column="1" Background="Transparent" Margin="0">
                                                        <Path x:Name="Arrow"
                          Fill="#5fc4b8"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Data="M0,0 L4,4 8,0 Z"/>
                                                    </Border>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Fill" Value="#7fd8cc" TargetName="Arrow"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Fill" Value="#7fd8cc" TargetName="Arrow"/>
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Fill" Value="#7fd8cc" TargetName="Arrow"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <ContentPresenter x:Name="ContentSite"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="10,5,30,5"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>

                                    <TextBox x:Name="PART_EditableTextBox"
                                         Style="{x:Null}"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Margin="10,5,30,5"
                                         Focusable="True"
                                         Background="Transparent"
                                         Visibility="Hidden"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"/>

                                    <Border x:Name="UnderlineBorder" Background="#5fc4b8" Height="2" VerticalAlignment="Bottom" Margin="0,0,0,-2" Opacity="0" />
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                        Background="#252c43"
                                        BorderBrush="#5fc4b8"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter Property="Opacity" Value="1" TargetName="UnderlineBorder" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Height" Value="0" TargetName="DropDownBorder" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#a0a8c0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ConfirmationItemStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ConfirmationLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a0a8c0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>

        <Style x:Key="ConfirmationValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#222a40" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="FileDocumentEdit" 
                                       Width="32" Height="32" 
                                       Foreground="#5fc4b8" 
                                       Margin="0,0,15,0"/>
                <TextBlock Text="СОЗДАНИЕ ЗАКАЗА ПРОЕКТА" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Steps Navigation -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="Step1Button" Content="1. Основная информация" 
                    Style="{StaticResource SecondaryButtonStyle}" IsEnabled="False"/>
            <Button x:Name="Step2Button" Content="2. Детали проекта" 
                    Style="{StaticResource SecondaryButtonStyle}" Margin="30,0,0,0"/>
            <Button x:Name="Step3Button" Content="3. Подтверждение" 
                    Style="{StaticResource SecondaryButtonStyle}" Margin="30,0,0,0"/>
        </StackPanel>

        <!-- Step Content -->
        <Grid Grid.Row="1" Background="#222a40" Margin="40,20">
            <!-- Step 1: Basic Info -->
            <StackPanel x:Name="Step1Panel" Visibility="Visible" Margin="20">
                <TextBlock Text="Основная информация" Style="{StaticResource SectionHeaderStyle}"/>

                <Label Content="Название проекта*" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ProjectNameTextBox" Style="{StaticResource ModernTextBoxStyle}"/>

                <Label Content="Описание проекта*" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ProjectDescriptionTextBox" Style="{StaticResource ModernTextBoxStyle}" 
                         Height="120" AcceptsReturn="True" TextWrapping="Wrap"/>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Label Content="Дата начала*" Style="{StaticResource LabelStyle}"/>
                        <DatePicker x:Name="StartDatePicker" Style="{StaticResource ModernDatePickerStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Label Content="Желаемая дата завершения" Style="{StaticResource LabelStyle}"/>
                        <DatePicker x:Name="EndDatePicker" Style="{StaticResource ModernDatePickerStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Step 2: Project Details -->
            <StackPanel x:Name="Step2Panel" Margin="30" Visibility="Collapsed">
                <TextBlock Text="Детали проекта" Style="{StaticResource SectionHeaderStyle}"/>

                <Label Content="Тип проекта*" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="ProjectTypeComboBox" Style="{StaticResource ComboBoxStyle}" Margin="0,10">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="White"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label Content="Бюджет (руб)" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="BudgetTextBox" Style="{StaticResource ModernTextBoxStyle}"/>

                <Label Content="Дополнительные требования" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="RequirementsTextBox" Style="{StaticResource ModernTextBoxStyle}" 
                         Height="120" AcceptsReturn="True" TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <CheckBox x:Name="UrgentCheckBox" Content="Срочный проект" 
                              Style="{StaticResource ModernCheckBoxStyle}" Margin="0,0,20,0"/>
                    <CheckBox x:Name="ConfidentialCheckBox" Content="Конфиденциальный проект" 
                              Style="{StaticResource ModernCheckBoxStyle}"/>
                </StackPanel>
            </StackPanel>

            <!-- Step 3: Confirmation -->
            <ScrollViewer x:Name="Step3Panel" Margin="30" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Подтверждение заказа" Style="{StaticResource SectionHeaderStyle}"/>

                    <Border Background="#252c43" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Проверьте введенные данные:" Foreground="#a0a8c0" Margin="0,0,0,10"/>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Название:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmNameText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Описание:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmDescriptionText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Дата начала:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmStartDateText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Дата завершения:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmEndDateText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Тип проекта:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmTypeText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Бюджет:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmBudgetText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>

                            <Grid Style="{StaticResource ConfirmationItemStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Дополнительно:" Grid.Column="0" Style="{StaticResource ConfirmationLabelStyle}"/>
                                <TextBlock x:Name="ConfirmOptionsText" Grid.Column="1" Style="{StaticResource ConfirmationValueStyle}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="После отправки заказа с вами свяжется менеджер для уточнения деталей." 
                               Foreground="#5a6278" FontSize="12" TextAlignment="Center" FontStyle="Italic"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Navigation Buttons -->
        <Border Grid.Row="2" Background="#1b2339" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton" Content="ОТМЕНА" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Left"
                        Click="CancelButton_Click"/>

                <Button x:Name="BackButton" Content="НАЗАД" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Right"
                        Margin="0,0,20,0"
                        Grid.Column="1"
                        Click="BackButton_Click"
                        Visibility="Collapsed"/>

                <Button x:Name="NextButton" Content="ДАЛЕЕ" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalAlignment="Right"
                        Grid.Column="2"
                        Click="NextButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>